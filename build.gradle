plugins {
    id 'application'
}

group 'we'

repositories {
    mavenCentral()
}

application {
    mainClass = 'app.Main'
}

task('printClasspath') {
    doFirst {
        println(sourceSets.main.runtimeClasspath.asPath)
    }
}
